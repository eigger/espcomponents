esphome:
  name: test-tcp-server

esp32:
  board: esp32dev
  framework:
    type: esp-idf

external_components:
  - source:
      type: local
      path: ../../../components
    components: [ tcp_server ]

wifi:
  ssid: "test_ssid"
  password: "test_password"

socket:

tcp_server:
  id: id_tcp
  port: 8888
  on_read:
    then:
      - lambda: |-
          ESP_LOGI("tcp", "Read %d bytes", len);
